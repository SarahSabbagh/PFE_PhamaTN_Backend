import { Theme } from "@mui/material";

export default function Stack(theme: Theme) {
  return {
    MuiStacKt: {
      styleOverrides: {
        root: {
          minHeight: "122px",
        },
      },
    },
  };
}
